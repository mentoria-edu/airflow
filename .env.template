# ==============================================================================
# AIRFLOW ENVIRONMENT VARIABLES
# ==============================================================================
# Copy this file to .env and fill in your values
# Command: cp .env.example .env
# ==============================================================================

# ------------------------------------------------------------------------------
# POSTGRES DATABASE
# ------------------------------------------------------------------------------
POSTGRES_USER=airflow
POSTGRES_PASSWORD=change_me
POSTGRES_DB=airflow

# ------------------------------------------------------------------------------
# AIRFLOW
# ------------------------------------------------------------------------------
# User UID (Linux: run 'echo $(id -u)' to get it)
AIRFLOW_UID=1000

# Fernet Key for encryption (REQUIRED)
# Generate a new one: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# SSH CONNECTION (Required for SSHOperator)
# IMPORTANT: SSH Key Setup
# 1. Generate SSH key pair (if not exists):
#    ssh-keygen -t ed25519 -f ${SSH_KEY} -N ""
# 
# 2. Copy the PUBLIC key to server authorized_keys:
#    ssh-copy-id -i ~/.ssh/id_ed25519_airflow.pub root@clientnode
#    OR manually:
#    cat ~/.ssh/id_ed25519_airflow.pub | ssh root@clientnode "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
#
# Format: ssh://[user]@[host]:[port]?key_file=[path]&no_host_key_check=[true|false]
AIRFLOW_CONN_SSH_CLIENTNODE=

# ------------------------------------------------------------------------------
# AIRFLOW WEB USER (Initial login)
# ------------------------------------------------------------------------------
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# ------------------------------------------------------------------------------
# PIP ADDITIONAL REQUIREMENTS (Optional)
# ------------------------------------------------------------------------------
# Examples:
# _PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-amazon
# _PIP_ADDITIONAL_REQUIREMENTS=apache-airflow-providers-amazon pandas boto3
_PIP_ADDITIONAL_REQUIREMENTS=